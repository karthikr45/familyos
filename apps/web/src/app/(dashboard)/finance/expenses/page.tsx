import { Placeholder } from '@/components/placeholder';

export default function ExpensesPage() {
  return (
    <Placeholder
      title="Expenses"
      description="Detailed, filterable transaction history with receipts."
    />
  );
}
